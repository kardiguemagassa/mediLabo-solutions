spring:
  datasource:
#    url: jdbc:tc:postgresql:16:///testdb
#    username: test
#    password: test
#    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    enabled: false

logging:
  level:
    root: WARN
    com.openclassrooms.patientservice: DEBUG